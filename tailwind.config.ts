import type { Config } from "tailwindcss";

// Tailwind CSS v4 uses CSS-first configuration in app/globals.css.
// Class detection is automatic; no `content` array is required.
const config: Config = {
  theme: {
    extend: {},
  },
};

export default config;
